name: Deploy Staging to Docker Hub

on:
  push:
    branches:
      - develop  # Runs only when the `develop` branch is updated

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}


      - name: Build and Push App Docker Image
        run: |
          docker build -f Docker/Dockerfile/app.Dockerfile -t najibalimudin/sipta-app:dev .
          docker push najibalimudin/sipta-app:dev

      - name: Build and Push Web Server Docker Image
        run: |
          docker build -f Docker/Dockerfile/web_server.Dockerfile -t najibalimudin/sipta-web_server:dev .
          docker push najibalimudin/sipta-web_server:dev
