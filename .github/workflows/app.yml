name: Docker Image CI

on:
  push:
    branches: [ "release/v.10" ]
  pull_request:
    branches: [ "release/v.10" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
      - build
      - push_docker_hub
      - pull
      - docker_compose
    # - uses: actions/checkout@v4
    # - name: Build the Docker image
    #   run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)